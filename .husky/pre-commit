#!/bin/sh

# Run lint-staged for syntax checking
npx lint-staged

# Run quick tests to ensure code correctness
echo "🧪 Running quick validation tests..."
npm run test:syntax

# Check if both servers can start
echo "🚀 Validating development environment..."
node scripts/validate-dev-env.js

echo "✅ Pre-commit checks passed!"
